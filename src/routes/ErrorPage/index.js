import './index.scss'

const ErrorPage = () => {
  const root = document.getElementsByClassName('root')[0]
  root.innerHTML = `<h1 class='errorpage-heading'>Error page</h1>`
}

export default ErrorPage
